import { NextFunction, Request, Response } from 'express'
import streamRouter from './routers/streamRouter'

describe('Stream Router Middleware', () => {
  let mockRequest
  let mockResponse
  let NextFunction = jest.fn()
})
